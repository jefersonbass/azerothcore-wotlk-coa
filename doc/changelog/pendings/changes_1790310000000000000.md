### Fixed

- Two of the Worldforged pickups of the **Elwynn Forest and Northshire Valley** pass stand where the
  map editor last left them, not where that pass wrote them. Both were moved again after the pass
  was written, and both are now asserted as the world holds them, position and rotation quaternion
  down to the digit:
  - **Ziz's Alchemy Goggles** (254229, guid 6941368) in Jasperlode Mine, 0.2 yd higher than the pass
    left it, its x and y nudged by a thousandth of a yard at the same time.
  - Northshire's **Cherry Pie prop** (90635, guid 6960002), back on the spot the pass before the
    last one wrote, 0.35 yd from where the last pass left it.
- Each row is written with an `UPDATE` keyed on its `guid` and touches nothing but the position, the
  rotation and the comment, so the migration is a no-op on a database that already carries the rows
  and no other column is disturbed.
